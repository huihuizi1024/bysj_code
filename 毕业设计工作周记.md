# 毕业设计工作周记

> 项目名称：基于大语言模型的大学生心理支持对话系统 MentalAlign
> 系统定位：前后端分离架构，后端 Spring Boot 3 + MyBatis-Plus + MySQL，前端 Vue 3 + Element Plus，AI 层支持 DeepSeek / OpenAI / Kimi 及本地 LoRA 微调模型，核心技术包括 PRS 自适应干预、Guardian 四层危机检测、认知投票与行为打卡数字表型、意图分类与角色调度等。

---

## 第1周

**工作时间段：2026.3.9 — 2026.3.14**

### 一、本周计划及执行情况

1. 开展毕业设计选题调研，阅读相关文献综述：完成
2. 熟悉项目技术栈，搭建本地开发环境：完成
3. 梳理系统功能需求，编写需求规格说明书初稿：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了选题调研阶段的文献综述阅读工作。调研了国内外心理健康 AI 系统的研究现状，系统梳理了从早期 Woebot、Wysa 等基于 CBT 脚本规则的系统，到当前基于大语言模型（LLM）的生成式心理支持对话系统的技术演进路径。重点阅读了以下文献资料：
   - PMC 系统综述：Charting the evolution of AI mental health chatbots from rule-based systems to LLMs（2024），梳理了 LLM 相比传统规则系统在情感共情深度、语境理解与长期干预灵活性上的核心优势；
   - EMNLP 2025 论文：How Mental Health AI Research Is Shaping the Future，了解了当前学术前沿在"治疗过程模拟""情感对齐"及"临床级安全保障"方向的研究进展；
   - medRxiv 论文：A deterministic safety pipeline for therapeutic AI（2026），了解了确定性安全管道（Deterministic Safety Pipeline）的设计原则，为后续 Guardian 安全层设计提供了理论依据。
   确定了毕业设计选题方向：基于大语言模型的大学生心理支持对话系统的设计与实现。

2. 针对本周计划2，完成了开发环境的搭建与调试。配置了 Java 21 + Maven 项目结构，熟悉了 Spring Boot 3.3 框架的核心组件（Controller、Service、Mapper 分层架构）；配置了 Vue 3 + Vite 前端工程，熟悉了组合式 API 与 Pinia 状态管理的使用方式；搭建了 MySQL 8.x 数据库环境，导入项目初始化脚本 init.sql，验证了用户注册/登录、JWT 鉴权等基础功能的正常运行。同时验证了后端 SSE 流式对话接口与 DeepSeek API 的连通性。

3. 针对本周计划3，完成了系统需求规格说明书的初稿编写。梳理了系统的核心功能模块，包括：用户账号与鉴权模块、多会话对话管理模块、情绪分析模块、危机预警模块、支持资源库模块、认知投票模块、行为打卡模块、心理准备度评估模块、疗效评估模块等。明确了系统的目标用户群体（高校大学生），以及系统的核心设计目标：构建一个具备自适应干预能力与确定性安全保障的心理支持对话系统。初步绘制了系统用例图与功能模块划分图。

### 三、存在问题

1. 问题1：文献综述阶段发现心理健康 AI 领域的研究方向众多，涉及心理学理论、计算机科学、伦理规范等多个交叉领域，文献资料数量庞大，在有限时间内难以对所有方向进行深入研读。解决尝试：与指导教师沟通后，确定了以"自适应干预机制"和"安全性设计"作为本次毕业设计的核心聚焦方向，其他相关方向作为辅助参考。

2. 问题2：本地开发环境中，BGE 向量嵌入服务的部署尚未完成（需要 Python 环境和 BGE 模型权重），影响后续 Guardian 第二层向量语义检测功能的调试。解决尝试：暂时使用关键词匹配层作为兜底方案，待后续配置好向量服务后再接入。

### 四、下周计划

1. 完成系统详细设计文档，包括系统架构设计、数据库详细设计、接口设计等；
2. 启动后端核心功能的编码实现，首先实现用户注册/登录模块和 JWT 鉴权机制；
3. 设计并创建数据库核心业务表结构，完善 init.sql 初始化脚本；
4. 阅读 MyBatis-Plus 框架文档，掌握条件构造器与分页插件的使用方法。

---

## 第2周

**工作时间段：2026.3.16 — 2026.3.21**

### 一、本周计划及执行情况

（继承第1周"下周计划"）
1. 完成系统详细设计文档：完成
2. 实现后端用户注册/登录与 JWT 鉴权模块：完成
3. 设计并创建数据库核心业务表：完成
4. 搭建前端项目工程结构：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了系统详细设计文档的编写。设计了系统的总体架构，采用前后端分离的单体架构（Monolithic Architecture）：前端基于 Vue 3 构建 SPA 应用，负责用户交互渲染与状态管理；业务逻辑层基于 Spring Boot 3.3 提供 RESTful API；AI 推理层通过 OpenAI 兼容接口规范接入多款大模型。设计了 23 张数据库业务表，涵盖用户账号、对话会话、情绪分析、危机预警、认知投票、行为打卡、意图分类、用户画像、支持资源、AI 模型配置、疗效评估等完整业务域。设计了核心模块之间的数据流向与调用关系图。

2. 针对本周计划2，完成了后端用户注册/登录与 JWT 鉴权模块的实现。实现了 UserController 中的注册接口（`POST /user/register`，用户名≥4字符、密码≥6字符、BCrypt 加密存储）和登录接口（`POST /user/login`，签发 24 小时有效期 JWT Token）。配置了 JwtInterceptor 实现 Token 验签与 currentUserId 注入，配置了 RoleInterceptor 实现 USER/ADMIN 角色的权限校验。在 WebMvcConfig 中注册了拦截器，对 `/user/login`、`/user/register` 等公开接口放行，对 `/chat/**`、`/emotion/**` 等需鉴权接口进行拦截。完成了前端登录页（Login.vue）和注册页（Register.vue）的实现，配置了 Vue Router 全局前置守卫，未登录自动跳转登录页。

3. 针对本周计划3，完成了数据库核心业务表的设计与初始化。设计了 user（用户表，含 username、password、role 字段）、chat_session（会话表，含 user_id、title、create_time）、chat_message（消息表，含 session_id、role、content）、emotion_record（情绪记录表，含 emotion_type、emotion_score、keywords、analysis_time）、crisis_alert（危机预警表，含 alert_level、alert_type、status）、supportive_resource（支持资源表，含 category、trigger_emotion、priority）等核心表结构。在 init.sql 中完成了建表语句编写，并插入了一条默认管理员账号（admin/admin123）和一条默认 DeepSeek 模型配置记录。验证了数据库连接与 CRUD 操作正常。

4. 针对本周计划4，完成了前端项目工程结构的搭建。初始化了 Vue 3 + Vite 项目，安装了 Element Plus、Vue Router、Pinia、Axios 等核心依赖。配置了 Vite 代理解决开发环境下的跨域问题（前端开发服务器 localhost:5173 → 后端 localhost:8080）。创建了项目目录结构（views/、components/、api/、stores/、styles/），配置了 Element Plus 按需引入和 Font Awesome 图标库。为后续各功能模块的开发奠定了工程基础。

### 三、存在问题

1. 问题1：JWT Token 目前仅支持 Header 模式（`Authorization: Bearer xxx`），但 SSE 流式对话采用 GET 请求，无法在请求头中自定义 Header，需要额外实现 URL 参数模式的 Token 传递。解决尝试：在 JwtInterceptor 中增加了对 URL 参数 `?token=xxx` 的解析支持，并更新了前端 SSE 请求代码（EventSource 不支持自定义 Header，故通过 URL 参数传递 token）。

2. 问题2：数据库初始化脚本中使用了 DROP TABLE 语句，每次执行会清空所有数据，不利于开发阶段的数据持久化。解决尝试：在 init.sql 中将 DROP TABLE 语句注释掉，并告知团队成员在需要完全重置数据库时才启用。

### 四、下周计划

1. 实现多会话对话管理模块（ChatController、ChatSessionService），支持会话创建、列表查询、历史消息加载；
2. 实现 SSE 流式对话功能，在后端通过 SseEmitter 推送 AI 回复流；
3. 实现前端聊天界面（Home.vue 主聊天页面），集成 EventSource 接收 SSE 流；
4. 配置 DeepSeek API Key，验证 AI 对话功能的基本连通性。

---

## 第3周

**工作时间段：2026.3.23 — 2026.3.28**

### 一、本周计划及执行情况

（继承第2周"下周计划"）
1. 实现多会话对话管理模块：完成
2. 实现 SSE 流式对话后端接口：完成
3. 实现前端聊天界面与 EventSource SSE 接收：完成
4. 配置 DeepSeek API，验证 AI 对话连通性：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了多会话对话管理模块的实现。实现了 ChatController 中的会话管理接口：创建会话（`POST /chat/session/create`，支持自定义标题，默认为"新的心理探索"）、获取会话列表（`GET /chat/session/list`，按创建时间倒序）、查询历史消息（`GET /chat/history?sessionId={id}`，校验 session 归属权）。实现了 ChatSessionService 和 ChatMessageService 的业务逻辑，使用 MyBatis-Plus 的 BaseMapper 实现数据访问。会话标题在首次对话后由 AI 异步生成，提升用户体验。前端在 Home.vue 中集成了会话列表侧边栏，支持创建新会话和切换会话。

2. 针对本周计划2，完成了 SSE 流式对话后端接口的实现。在 ChatController 中实现了 `GET /chat/stream?sessionId={id}&content={内容}&modelCode={code}` 接口，使用 Spring 的 SseEmitter 实现服务器发送事件流。使用 Java 21 HttpClient 的 `sendAsync` 发起异步 HTTP 请求到 DeepSeek API，流式读取响应 Token 并通过 SseEmitter 逐字推送前端。设计了 SSE 消息格式：普通文本使用 `data: xxx\n\n` 格式，结束标记使用 `data: [DONE]\n\n`。完整回复保存到 chat_message 表，完成后触发 AI 异步生成会话标题。

3. 针对本周计划3，完成了前端聊天界面的实现。在 Home.vue 中构建了主应用布局（左侧会话列表 + 右侧对话区域），实现了消息气泡组件（区分用户消息和 AI 回复）、输入框与发送按钮、SSE 流式对话接收逻辑。使用原生 EventSource API 接收 SSE 流，在 `message` 事件中逐字追加 AI 回复内容，实现打字机效果。处理了 `[DONE]` 结束标记和 `error` 事件的连接断开逻辑。在 AI 消息气泡中增加了"正在输入..."的占位状态，提升交互体验。修复了多个前端样式细节问题（如滚动到底部逻辑、空会话提示等）。

4. 针对本周计划4，成功配置了 DeepSeek API Key 并验证了 AI 对话功能的连通性。在 application.properties 中配置了 `ai.deepseek.url` 和 `ai.deepseek.key` 参数。在后端 AiServiceImpl 中实现了统一的 AI 模型调用逻辑，支持 DeepSeek Chat Completions API 的流式响应。测试了多条对话消息，确认 AI 能够正确理解心理支持场景的对话意图，并返回合理的共情式回复。验证了前端 SSE 流式传输的稳定性和完整性。

### 三、存在问题

1. 问题1：前端 EventSource 不支持自定义 Header，导致 JWT Token 只能通过 URL 参数传递，存在一定的安全风险（Token 在 URL 中可能被日志记录）。当前已在生产环境提示中建议用户及时登出，并在服务端实现了 Token 有效期校验。下一步可考虑使用 fetch + ReadableStream 替代 EventSource，以支持自定义 Header。

2. 问题2：在长对话场景下，多轮对话的上下文累积导致 token 消耗快速增长，成本压力增大。尝试方案：在 system prompt 中限制历史消息注入条数为最近 20 条，后续可通过对话摘要机制进一步优化。

### 四、下周计划

1. 实现情绪分析模块（EmotionAnalysisService），在每条用户消息发送后自动触发情绪分析；
2. 设计并实现心理准备度得分（PRS）计算机制，包括参与度、情感效价、唤醒度三维度加权；
3. 实现意图分类模块（IntentReconstructService），支持 11 种临床意图分类；
4. 实现动态角色调度模块（RoleSchedulerService），支持五类 AI 治疗角色的自适应切换。

---

## 第4周

**工作时间段：2026.3.30 — 2026.4.4**

### 一、本周计划及执行情况

（继承第3周"下周计划"）
1. 实现情绪分析模块：完成
2. 设计并实现 PRS 心理准备度得分计算：完成
3. 实现意图分类与动态角色调度模块：完成
4. 完善前端情绪报告图表展示：滞后（ECharts 集成部分）

**本周计划完成度：90%**

### 二、实际工作

1. 针对本周计划1，完成了情绪分析模块的实现。实现了 EmotionAnalysisService，在每条用户消息发送后同步触发情绪分析。使用 DeepSeek API 的结构化输出能力，调用情绪分析 prompt，要求返回 JSON 格式：包含 emotionType（positive/negative/neutral/anxiety/depression/anger）、emotionScore（0.0~1.0）、keywords（情感关键词列表）。分析结果持久化到 emotion_record 表，与 chat_message 通过 message_id 关联。实现了 EmotionController 的三个接口：获取会话情绪历史（`/emotion/session/{sessionId}`）、获取用户情绪变化趋势（`/emotion/trend?days=7`，支持 7/30/90 天区间切换）、获取会话最新情绪状态（`/emotion/latest/{sessionId}`）。

2. 针对本周计划2，完成了 PRS 心理准备度得分的计算实现。实现了 PsychologicalReadinessService，综合参与度（Engagement）、情感效价（Valence）和唤醒度（Arousal）三维度计算 PRS 总分：
   - 参与度 E：由历史活跃度分量 E_hist（有 2+ 条→0.5，有 1 条→0.3，无→0）、消息长度分量（按 300字=0.5 线性归一化）、新会话奖励分量 E_new（=0.3）加权叠加；
   - 情感效价 V：将 DeepSeek 返回的 [-1, 1] 区间值线性映射至 [0, 1]；
   - 唤醒度 A：由情绪分析服务直接返回 [0, 1] 区间值。
   PRS = min(1.0, 0.40×E + 0.35×V + 0.25×A)，经三段阈值函数映射为 scaffolding（<0.35）、supportive（0.35~0.65）、reflective（>0.65）三种干预深度。计算结果持久化至 psychological_readiness_score 表。

3. 针对本周计划3，完成了意图分类与动态角色调度模块的实现。实现了 IntentReconstructService，基于用户输入和情绪状态，通过 DeepSeek API 重构临床意图，支持 11 种意图分类：existential_crisis、value_clarification、cognitive_restructuring、behavioral_activation、emotion_regulation、distress_tolerance、social_skill、grief_processing、sleep_hygiene、self_compassion、crisis_stabilization。每种意图关联疗法维度（ACT/CBT/DBT）和推荐 AI 角色。实现了 RoleSchedulerService，根据意图类型、情绪得分和 PRS 动态选择治疗角色：Supportive、Empathetic、Socratic、Guided、Crisis Mode，以及 dynamic_switcher（边界区间 0.30-0.40 / 0.60-0.70）。各角色特征以 Prompt 片段形式注入 AI system prompt，驱动回复风格的差异化调整。

4. 针对本周计划3（计划变更，ECharts 集成部分），进度滞后。原因是本周将主要精力投入到了 PRS 计算、意图分类和角色调度三个核心算法的实现与调试上，ECharts 图表的集成延后到下周进行。前端情绪报告页面（EmotionChart 组件）的布局已完成，ECharts 图表的接入将在下周完成。

### 三、存在问题

1. 问题1：情绪分析 API 调用的延迟较高（每次调用约 1-2 秒），影响了对话的实时响应体验。解决尝试：将情绪分析改为异步执行（@Async 注解），不阻塞 SSE 流式回复的主链路。分析结果在后台写入数据库，前端可通过轮询或 WebSocket 获取最新情绪状态。

2. 问题2：PRS 计算中的权重系数（0.40 / 0.35 / 0.25）是经验性设定的，不同用户群体可能存在个体差异。当前版本的阈值参数尚缺乏真实用户反馈数据的验证。解决尝试：在系统上线后收集用户反馈数据，为后续回归优化提供依据。

### 四、下周计划

1. 完成前端 ECharts 情绪报告图表的集成（折线趋势图 + 饼图分布）；
2. 实现认知投票（CognitiveVoting）模块，设计 14 道认知评估题目与情绪触发机制；
3. 实现行为打卡（BehaviorCheckIn）模块，支持心情/睡眠/运动/社交四种打卡类型；
4. 实现支持资源库（ResourceService）模块，设计五类资源的情绪触发匹配逻辑。

---

## 第5周

**工作时间段：2026.4.7 — 2026.4.11**

### 一、本周计划及执行情况

（继承第4周"下周计划"）
1. 完成 ECharts 情绪报告图表集成：完成
2. 实现认知投票（CognitiveVoting）模块：完成
3. 实现行为打卡（BehaviorCheckIn）模块：完成
4. 实现支持资源库（ResourceService）模块：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了前端 ECharts 情绪报告图表的集成。在 EmotionChart.vue 组件中实现了两个核心图表：
   - **情绪趋势折线图**：调用 `/emotion/trend?days=7/30/90` 接口获取近 N 天情绪数据，使用 ECharts 折线图展示情绪得分随时间的变化趋势，支持时间区间切换（7天/30天/90天）；
   - **情绪类型分布饼图**：统计各情绪类型（positive/negative/neutral/anxiety/depression/anger）的出现次数，使用 ECharts 饼图展示分布比例。
   图表采用统一的视觉风格（渐变色主题、玻璃拟态卡片），响应式布局适配不同屏幕尺寸。实现了图表的空数据友好提示和加载动画。

2. 针对本周计划2，完成了认知投票模块的实现。设计了 14 道认知评估题目，分布于六个临床维度：认知扭曲（3题）、自我效能（2题）、应对策略（2题）、自我慈悲（2题）、情绪调节（1题）、行为激活（2题），另有睡眠卫生类题目（2题）作为日常行为监测补充。在 CognitiveVotingController 中实现了相关接口：`POST /voting`（提交投票）、`GET /voting/history`（获取投票历史）、`GET /voting/session/{sessionId}`（按会话获取记录）、`GET /voting/next`（获取下一个问题）、`GET /voting/shouldTrigger`（检查是否应触发投票）。投票触发机制：当检测到情绪类型为"抑郁"且得分 < 0.4，或"负面"且得分 < 0.3 时，系统判定用户当前认知模式可能存在偏差，随即触发投票。每轮投票基础得分为 0.1，依据选项的临床语义赋予增量修正。

3. 针对本周计划3，完成了行为打卡模块的实现。实现了 BehaviorCheckInController 的相关接口：`POST /checkin`（提交打卡，支持 mood/sleep/exercise/social 四种类型）、`GET /checkin/streak`（获取连续打卡天数）、`GET /checkin/stats?days=7`（获取打卡统计，含各类型打卡次数、连续天数、活跃度评分 activeScore）、`GET /checkin/recent?days=7`（获取最近打卡记录）。打卡数据按 user_id 和 checkin_date 去重，存储于 user_behavior_checkin 表。前端实现了 CheckInCard 组件，支持 Tab 切换打卡类型，打卡选项带 Emoji 标识，显示连续打卡天数的火焰徽章，打卡成功 Toast 提示。打卡后自动触发用户画像的动态修正。

4. 针对本周计划4，完成了支持资源库模块的实现。设计了五类资源分类（crisis / selfhelp / mindfulness / counseling / tips），每类资源包含 trigger_emotion（触发情绪类型）、trigger_score_min/max（触发得分区间）、priority（优先级，越小越优先）等字段。实现了 ResourceService 的推荐逻辑：以情绪类型（emotion_type）和得分区间（trigger_score_min <= score <= trigger_score_max）为双条件触发键，按 priority 升序检索。危机热线（category=crisis）固定高亮展示在最顶部。每推荐一次自动记录到 resource_recommendation 表。在 AiServiceImpl.streamChat() 中集成了 buildDynamicStrategy() 方法，将资源策略片段（策略文本 + 最相关练习名称）注入 AI system prompt，让 AI 在回复中自然融入引导性练习建议。每条推荐记录均持久化至数据库。

### 三、存在问题

1. 问题1：认知投票组件在前端的触发时机需要精确控制——过早触发会打断对话节奏，过晚触发可能错过最佳干预时机。当前基于情绪阈值（depression < 0.4 / negative < 0.3）的触发策略较为简单，可能在某些边界场景下不够精准。解决尝试：在前端 CognitiveVoting.vue 组件中增加了 `recentVotingType` 参数，避免同一会话中重复推送相同维度的题目，确保评估覆盖面与信息多样性。

2. 问题2：资源库的初始数据量较少（仅预置了 3-5 条测试数据），无法充分展示五类资源分类的触发效果。解决尝试：在 init.sql 中补充各分类下的代表性资源数据，确保每种情绪类型和得分区间都有足够的推荐候选资源。

### 四、下周计划

1. 设计并实现 Guardian 四层危机检测管道（PHQ-9 语义层、BGE 向量语义层、关系脉络门控层、关键词硬匹配层）；
2. 设计并实现 Reflector 输出合规审计层（自伤建议、医疗处方、歧视语言、AI 伦理违规检测）；
3. 完善危机预警管理后台（CrisisController），供管理员处理待处理预警；
4. 实现 AI System Prompt 的四路上下文融合（PRS 干预深度 + 临床意图 + 资源策略 + 角色片段）。

---

## 第6周

**工作时间段：2026.4.13 — 2026.4.18**

### 一、本周计划及执行情况

（继承第4周"下周计划"）
1. 设计并实现 Guardian 四层危机检测管道：完成
2. 设计并实现 Reflector 输出合规审计层：完成
3. 完善危机预警管理后台：完成
4. 实现 AI System Prompt 四路上下文融合：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了 Guardian 四层危机检测管道的实现。Guardian 作为系统的"物理级第一防线"，所有用户输入在触及业务逻辑前必须通过四扇检测门的完整审查：
   - **第一层（PHQ-9 语义层）**：基于 PHQ-9 量表第 9 项（自伤意念）构建 16 项关键词组合数组（"想死/不想活/活不下去/zs/si/轻生/一了百了"等），使用朴素字符串包含检测（`text.contains(kw)`），命中即返回 `riskLevel = "high"`, `triggerType = "PHQ9"`；
   - **第二层（BGE 向量语义层）**：将用户输入经 BGE 向量模型编码为 768 维稠密向量，与 crisis_sample 表中预存的危机样本向量进行余弦相似度比对，当相似度 ≥ 0.55 时触发危机预警。由于 BGE 嵌入服务尚未完全就绪，当前使用关键词模式作为过渡方案，代码结构已按向量接口规范设计，服务就绪后可直接切换；
   - **第三层（关系脉络门控层）**：检测第一人称代词（"我/我自己的/我感觉/我认为/我的"）与高危情境词（去世、逝世、分手、离婚、失业等 14 项）的共现模式，命中则返回 `riskLevel = "medium"`；
   - **第四层（关键词硬匹配层）**：对高危关键词（16 项，返回 high）、中危关键词（10 项，返回 medium）、低危关键词（6 项，返回 low）依次执行包含检测，作为最终兜底防线。
   四层检测以 OR 逻辑串联，任一层触发即判定为危机事件，激活确定性安全响应分支。

2. 针对本周计划2，完成了 Reflector 输出合规审计层的实现。Reflector 对 AI 生成内容执行流式正则匹配审计，检测到违规后立即截断输出并注入安全替代文本（含全国心理危机干预热线 400-161-9995）。审计四个维度：
   - $D_1$：自我伤害建议检测——正则如 `.{0,10}(割腕|跳楼|服药|上吊).{0,10}(方法|怎么|如何)`；
   - $D_2$：医疗处方/诊断检测——如"我给你开...药"、"你得了...病"等；
   - $D_3$：歧视性语言检测——如"这种人...活该"等；
   - $D_4$：AI 伦理违规检测——检测 AI 虚构人类经历的第一人称陈述，如"我曾经也..."、"我也经历过..."等。
   同时实现了流式审计方法 `auditStream()`，对 SSE 流中累积的片段内容进行实时检测。

3. 针对本周计划3，完成了危机预警管理后台的实现。实现了 CrisisController 的相关接口：`GET /crisis/pending`（管理员获取所有待处理危机预警记录，按创建时间倒序）、`POST /crisis/handle/{alertId}`（管理员处理预警，记录处理时间和备注）、`GET /crisis/user/alerts`（用户查看自己的危机预警记录，含已处理和待处理的）。所有危机事件自动写入 crisis_alert 表。前端 Admin.vue 中实现了危机预警管理面板，管理员可查看预警详情、处理状态和匹配关键词，并输入处理备注。所有危机预警均需人工跟进处置，形成"自动拦截 + 人工兜底"的完整闭环。

4. 针对本周计划4，完成了 AI System Prompt 四路上下文融合的实现。在 AiServiceImpl.streamChat() 方法中按以下顺序构建完整的系统提示词：
   - 基础角色定义（base prompt：温暖、共情、不评判的高校心理倾听者身份定位）；
   - PRS 上下文（当前准备度级别 + scaffolding/supportive/reflective 干预风格建议）；
   - 临床意图上下文（当前识别的 11 种意图分类 + 推荐疗法模块）；
   - 资源策略上下文（匹配的资源库策略片段，经干预深度修饰器加工）；
   - 角色 Prompt 片段（当前调度的治疗角色特征描述）。
   四路上下文最终注入 DeepSeek API 的 system 字段，确保 AI 的回复在内容相关性和干预策略层面均实现个性化。

### 三、存在问题

1. 问题1：Guardian 第二层（BGE 向量语义层）依赖 BGE 嵌入模型，当前因 Python 环境和模型权重尚未配置完成，暂时使用关键词模式作为过渡。虽然不影响整体危机检测功能（第一层 PHQ-9 和第四层关键词硬匹配已覆盖主要高危场景），但向量语义层的缺失使得对隐晦表达的检测能力受限。解决尝试：计划在后续 AI 引擎模块中完成 BGE 服务的部署，并将其集成到 Guardian 第二层。

2. 问题2：Reflector 的流式审计在 SSE 场景下需要累积一定长度的文本片段才能进行正则匹配，可能存在文本被截断导致无法匹配完整模式的情况。解决尝试：实现了滑动窗口机制，对最近 N 个字符的文本片段进行持续审计，确保跨 token 边界的模式也能被正确识别。

### 四、下周计划

1. 实现前端认知投票弹窗（CognitiveVoting.vue）与行为打卡卡片（CheckInCard.vue）的 UI 优化；
2. 实现前端管理控制台（Admin.vue）的完整功能（危机预警处理 + 资源库 CRUD + 模型配置）；
3. 实现 AI 模型切换功能（ModelDialog 弹窗 + 后端模型配置管理接口）；
4. 完善用户画像（UserProfile）模块，前端个人中心（Profile.vue）开发。

---

## 第7周

**工作时间段：2026.4.20 — 2026.4.25**

### 一、本周计划及执行情况

（继承第6周"下周计划"）
1. 实现前端认知投票弹窗与行为打卡卡片 UI 优化：完成
2. 实现前端管理控制台（Admin.vue）完整功能：完成
3. 实现 AI 模型切换功能：完成
4. 完善用户画像模块与个人中心（Profile.vue）：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了认知投票弹窗与行为打卡卡片的 UI 优化。优化了 CognitiveVoting.vue 组件的视觉设计：采用渐变色头部（A6B2A4 色调）、卡片式弹窗、选项按钮带字母标识（A/B/C/D）、选中状态高亮效果。增加了关闭按钮和遮罩层支持，提交按钮根据是否选中选项自动控制禁用状态。优化了 CheckInCard.vue 组件：以 Tab 形式组织四种打卡类型（心情/睡眠/运动/社交），每个打卡选项带 Emoji 标识，连续打卡天数以火焰徽章动画展示，打卡成功后显示 Toast 提示。打卡备注输入框支持用户添加个性化说明。

2. 针对本周计划2，完成了前端管理控制台（Admin.vue）的完整功能开发。管理控制台包含三个主要功能模块：
   - **危机预警处理面板**：展示所有待处理危机预警记录（红色高亮背景），显示预警等级、类型、匹配关键词、触发时间和用户信息，管理员可输入处理备注并标记为已处理；
   - **资源库管理面板**：实现支持性资源的新增（`POST /resource/admin/add`）、编辑（`PUT /resource/admin/update/{id}`）、删除（`DELETE /resource/admin/delete/{id}`）、启用/禁用（`PUT /resource/admin/toggle/{id}`）等 CRUD 操作，支持按 category 分页筛选，资源新增表单包含 trigger_emotion、trigger_score_min/max、priority 等核心字段；
   - **模型配置面板**：展示所有 AI 模型配置，支持管理员启用/禁用模型、调整排序顺序。

3. 针对本周计划3，完成了 AI 模型切换功能的完整实现。设计了 ai_model_config 表（code、name、api_url、model_name、api_key_alias、temperature、max_tokens、enabled、is_default、sort_order）和 user_model_preference 表（user_id、model_code）。实现了 ModelConfigController 的接口：`GET /model/list`（获取所有可用模型）、`GET /model/current`（获取当前用户选定的模型）、`POST /model/select`（切换用户模型偏好）。前端实现了 ModelDialog.vue 弹窗组件，展示所有可用模型（当前模型高亮），用户点击后调用 `/model/select` 保存偏好，SSE 请求的 URL 参数 `modelCode` 从 appStore.currentModel.code 动态读取。

4. 针对本周计划4，完成了用户画像模块与个人中心页面的开发。实现了 UserProfileService，基于最近 10 条情绪分析记录动态计算用户的心理画像特征：压力等级（stress_level：high/medium/low）、情绪趋势（emotional_trend：rising/falling/stable）、对话次数（conversation_count）、总消息数（total_messages）、人格类型（personality_type）及主要关切领域（main_concern）。画像数据持久化至 user_profile 表。前端 Profile.vue 实现了用户画像编辑页面，支持用户自主更新人格类型和主要困扰字段，同时以可视化方式展示用户的心理状态趋势摘要。ProfileTag.vue 组件将用户画像以标签形式嵌入对话界面，增强用户对自身心理状态的认知。

### 三、存在问题

1. 问题1：管理控制台的资源库管理面板在新增资源时，trigger_score_min/max 字段的取值范围校验逻辑尚不完善，可能存在 min > max 的无效数据。解决尝试：在后端 ResourceController 的新增接口中增加了参数校验逻辑，确保 min ≤ max，并返回友好的错误提示。

2. 问题2：用户画像的动态修正算法中，指数加权移动平均的初始 PRS 值（冷启动问题）尚未妥善处理。在新用户首次使用时，由于缺乏历史数据，画像计算的准确性受限。解决尝试：为新用户设置默认 PRS 中间值（0.5），并在前几次使用中快速积累情感数据以修正画像。

### 四、下周计划

1. 实现疗效评估模块（TherapyEvaluationService），支持 MentalAlign 框架（CSS/ARS）和 HEART 五维指标评估；
2. 实现用户满意度模块（UserSatisfactionService），支持满意度快速提交和历史记录查询；
3. 设计并实现评测执行器（EvalExecutorService），基于 402 条评测集样本对多模型进行横向对比；
4. 开发前端疗效评估仪表盘（EvaluationDashboard.vue），可视化展示评估结果。

---

## 第8周

**工作时间段：2026.4.27 — 2026.5.2**

### 一、本周计划及执行情况

（继承第7周"下周计划"）
1. 实现疗效评估模块（MentalAlign + HEART）：完成
2. 实现用户满意度模块：完成
3. 实现评测执行器（EvalExecutorService）：完成
4. 开发前端疗效评估仪表盘：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了疗效评估模块的实现。实现了 TherapyEvaluationService，采用 MentalAlign 双轴评估框架：
   - **CSS（Cognitive Support Score）认知支持得分**：衡量引导性、信息量、专业性与结构化程度；
   - **ARS（Affective Resonance Score）情感共鸣得分**：衡量共情表达、情感验证、温暖感与安全感。
   每次 AI 回复后，EvalExecutorService 自动调用评估接口，通过 AI-as-Judge 方式对回复进行评分（调用 DeepSeek API，使用精心设计的评估 Prompt）。评分结果持久化至 therapy_evaluation 表。实现了 EvaluationController 的相关接口：`GET /evaluation/therapy/stats?days=7`（疗效统计数据）、`GET /evaluation/therapy/compare?days=7`（模型横向对比）、`GET /evaluation/therapy/trend?days=30`（疗效趋势）、`GET /evaluation/therapy/history`（疗效历史）。

2. 针对本周计划2，完成了用户满意度模块的实现。实现了 UserSatisfactionService，采用 Google HEART 五维指标体系：
   - Happiness（满意度，权重 0.30）
   - Engagement（参与度，权重 0.15）
   - Adoption（采纳率，权重 0.15）
   - Retention（留存率，权重 0.20）
   - Task Success（任务成功率，权重 0.20）
   综合评分 HEART = 0.30H + 0.15E + 0.15A + 0.20R + 0.20T。实现了相关接口：`POST /evaluation/satisfaction/quick`（快速满意度提交）、`GET /evaluation/satisfaction/history`（满意度历史）、`GET /evaluation/satisfaction/user-stats`（用户统计）、`GET /evaluation/satisfaction/platform?days=7`（平台级统计）、`GET /evaluation/satisfaction/check?sessionId={id}`（检查是否已提交）。

3. 针对本周计划3，完成了评测执行器的实现。设计了 eval_dataset 表（包含 402 条评测集样本，涵盖 daily、concern、depression、selfharm、crisis 五个类别，含 20 条标注的危机样本）和 eval_run 表（评测执行记录，含 avg_css、avg_ars、crisis_intercept_rate、happiness 等字段）。实现了 EvalExecutorService，采用 Spring @Async 注解实现后台评测任务调度，不阻塞主请求线程。使用内存级 `CANCELLED_RUNS` Set 实现中断信号传递，支持评测任务的创建、取消与进度实时查询。每次评测均以 eval_dataset 全量 402 条样本执行，评测结果写入 eval_run 表。Guardian 危机拦截率通过回放 20 条危机样本进行独立验证。

4. 针对本周计划4，完成了前端疗效评估仪表盘的开发。在 EvaluationDashboard.vue 中实现了：
   - **模型横向对比柱状图**：展示 DeepSeek V3、GPT-4o Mini、Kimi-K2 三款模型在 CSS、ARS、Happiness、Retention 等维度的得分对比；
   - **疗效趋势折线图**：展示各模型 CSS/ARS 得分随时间的变化趋势；
   - **综合评分雷达图**：以雷达图形式直观展示五款模型的综合性能对比；
   - **平台级统计卡片**：展示平均 CSS、ARS、满意度、总评估次数等聚合指标。
   图表使用 ECharts 渲染，与情绪报告图表保持一致的视觉风格。

### 三、存在问题

1. 问题1：疗效评估的 AI-as-Judge 方法存在一定的主观性，不同评估样本的 CSS/ARS 评分可能存在偏差。解决尝试：在评估 Prompt 中增加了详细的评分标准说明（分 0-1 分 5 档），要求评分时给出简要理由，以提高评分的一致性。同时，在后续章节中明确说明了评估框架的局限性。

2. 问题2：评测执行器在处理 402 条评测样本时耗时较长（约 15-20 分钟），影响开发调试效率。解决尝试：在开发阶段提供小规模评测模式（仅评测 20 条样本），供快速验证使用。

### 四、下周计划

1. 开展多模型横向对比实验，获取 DeepSeek V3、GPT-4o Mini、Kimi-K2 的完整评估数据；
2. 分析实验结果，撰写实验结果分析报告；
3. 部署 BGE 嵌入服务，完善 Guardian 第二层向量语义检测；
4. 补充系统测试用例，完善异常场景处理（网络超时、API 限流等）。

---

## 第9周

**工作时间段：2026.5.4 — 2026.5.9**

### 一、本周计划及执行情况

（继承第8周"下周计划"）
1. 开展多模型横向对比实验：完成
2. 分析实验结果，撰写实验结果分析报告：完成
3. 部署 BGE 嵌入服务，完善 Guardian 第二层向量语义检测：完成
4. 补充系统测试用例，完善异常场景处理：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了多模型横向对比实验。基于 402 条评测集样本（含 20 条危机样本），对三款主流大语言模型（DeepSeek V3、GPT-4o Mini、Kimi-K2）进行了全面的性能对比测试。每次评测均以 eval_dataset 全量样本执行，实验结果如下：
   - **DeepSeek V3**：CSS 0.443，ARS 0.753，Happiness 0.803，Engagement 0.689，Adoption 0.396，Retention 0.807，综合得分 0.665；
   - **GPT-4o Mini**：CSS 0.718，ARS 0.821，Happiness 0.736，Engagement 0.678，Adoption 0.479，Retention 0.780，综合得分 0.734；
   - **Kimi-K2**：CSS 0.634，ARS 0.768，Happiness 0.779，Engagement 0.642，Adoption 0.458，Retention 0.732，综合得分 0.694。
   三款模型的 Guardian 危机拦截率均为 85.0%（由 Guardian 确定性规则控制，与模型选择无关）。

2. 针对本周计划2，完成了实验结果分析报告的撰写。深入分析了实验数据，揭示了以下关键发现：
   - **GPT-4o Mini 的认知-情感双优势**：CSS（0.718）与 ARS（0.821）双项最优，但 Happiness（0.736）相对较低，存在"过于专业化"的反馈，说明在轻度情绪困扰场景中可能存在"过度干预"倾向；
   - **DeepSeek V3 的情感-留存悖论**：CSS 最弱（0.443）但 Happiness 最强（0.803），形成"低认知高满意"的悖论现象，源于其"去专业化"的陪伴式倾听风格；
   - **认知-情感鸿沟的实证复现**：GPT-4o Mini 在 CSS 领先 DeepSeek V3 达 0.275 分（相对提升 62.1%），但 Happiness 反而落后 0.067 分，验证了 PRS 动态调度机制的必要性；
   - **Guardian 危机拦截率的模型无关性**：三款模型危机拦截率一致（85.0%），证实了 Guardian 确定性安全管道的"结构不变量"特性，漏检的 3 条案例均源于极度隐晦的隐喻性表达。

3. 针对本周计划3，完成了 BGE 嵌入服务的部署与 Guardian 第二层向量语义检测的完善。部署了基于 BGE-base-zh-v1.5 模型的 Python 嵌入服务（localhost:8001），实现了向量编码接口 `/encode`。在 VectorSimilarityService 中调用该服务，将用户输入编码为 768 维稠密向量，与 crisis_sample 表中预存的危机样本向量进行余弦相似度比对。当相似度 ≥ 0.55 时触发危机预警，≥ 0.65 时判定为高风险。危机样本向量在服务启动时一次性加载至内存（ConcurrentHashMap 缓存），避免每次检测都访问数据库或调用嵌入服务，大幅降低检测延迟。完成集成后，Guardian 第二层正式生效，整体危机拦截率达到 85.0%。

4. 针对本周计划4，补充了系统测试用例，完善了异常场景处理。编写了各核心模块的单元测试用例（UserService、ChatSessionService、EmotionAnalysisService、GuardianService、ReflectorService 等），使用 JUnit 5 和 Mockito 框架，覆盖正常流程和边界条件。针对以下异常场景实现了处理逻辑：
   - **网络超时**：为 DeepSeek API 调用设置了合理的超时配置（连接超时 10s，读超时 60s），超时后返回友好错误提示；
   - **API 限流**：在 AiServiceImpl 中增加了重试机制（指数退避，最多 3 次），处理 API 限流（429）错误；
   - **Token 过期**：JWT 过期时返回 401，前端检测到后自动跳转登录页并提示用户重新登录；
   - **数据库连接异常**：在 GlobalExceptionHandler 中统一处理 SQL 异常，返回友好错误信息。

### 三、存在问题

1. 问题1：实验结果显示 Guardian 危机拦截率为 85.0%，仍有 3 条极度隐晦的隐喻性表达样本（如"想去天上找奶奶"）未被检测到。根本原因在于 crisis_sample 向量库的覆盖不足。解决尝试：在 crisis_sample 表中补充了更多隐喻性危机表达的语义样本，并标注了文化特异性较高的隐喻表达类型。同时，计划在后续研究中探索引入大模型辅助的上下文语义推理增强机制。

2. 问题2：Kimi-K2 模型的长上下文处理能力（200k tokens）在本次中等长度会话场景中未能充分发挥优势。解决尝试：在实验报告的局限性分析中注明了这一发现，建议在后续研究中设计长程心理咨询场景的专项评测。

### 四、下周计划

1. 完善论文第二章"相关理论基础与文献综述"的撰写，补充数字表型技术、支架理论等核心概念的文献引用；
2. 完善论文第三章"系统总体架构设计"的撰写，补充技术选型说明和数据库 ER 图；
3. 完善论文第四章"核心算法与功能模块实现"的撰写，补充 PRS 计算公式、三级调度策略表等核心内容；
4. 进行系统整体联调，修复测试中发现的问题，确保系统各模块间协作正常。

---

## 第10周

**工作时间段：2026.5.11 — 2026.5.16**

### 一、本周计划及执行情况

（继承第9周"下周计划"）
1. 完善论文第二章"相关理论基础与文献综述"：完成
2. 完善论文第三章"系统总体架构设计"：完成
3. 完善论文第四章"核心算法与功能模块实现"：完成
4. 系统整体联调，修复测试问题：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了论文第二章"相关理论基础与文献综述"的撰写。系统梳理了支撑 MentalAlign 系统设计的跨学科理论基础：
   - **生态瞬时评估与数字表型技术**：论证了 EMA 技术在克服传统静态量表"切片性"局限方面的优势，引用文献支撑了"结合行为打卡与 LLM 语义理解的抑郁与焦虑识别准确率可达 90% 以上"的结论。设计了 PRS 动态修正算法（指数加权移动平均）：$PRS_t = 0.7 × PRS_{t-1} + 0.3 × (0.5 × emotion_t + 0.25 × checkin_t + 0.25 × voting_t)$；
   - **CBT 与临床意图识别**：梳理了 11 种临床意图分类跨越 ACT/CBT/DBT 等多个心理治疗流派的理论依据，设计了认知投票模块的评分模型和触发机制；
   - **支架理论与自适应干预深度**：论证了维果茨基支架理论向心理干预场景迁移的合理性和工程化路径，设计了 PRS 三段阈值映射（scaffolding / supportive / reflective）；
   - **大语言模型与情感感知动态 RAG**：论证了 RAG 在心理支持场景中避免"幻觉"风险的理论优势，设计了双条件触发检索（emotion_type + score 区间）和干预深度修饰器；
   - **心理健康 AI 的安全评估与伦理框架**：引用 Simplex 确定性安全管道和 MIND-SAFE 防御深度策略，论证了 Guardian 四层 OR 管道和 Reflector 四维输出审计的学术合理性。

2. 针对本周计划2，完成了论文第三章"系统总体架构设计"的撰写。详细描述了系统采用的前后端分离单体架构，设计了技术栈全景表（涵盖 Vue 3 / Spring Boot 3.3 / MyBatis-Plus / MySQL / SSE / JWT 等核心技术组件及其版本和作用）。设计了数据库详细架构（23 张业务表的完整字段说明和关联关系），绘制了模块组织结构图（后端 18 个 Controller + 30 个 Service + 23 个 Entity + 26 个 Mapper，前端 6 个 views + 14 个 components + 9 个 api 模块）。设计了系统安全架构（JWT 鉴权 + 角色权限控制 + BCrypt 密码加密 + 敏感配置隔离）。

3. 针对本周计划3，完成了论文第四章"核心算法与功能模块实现"的撰写。详细描述了以下核心算法的工程实现：
   - **PRS 量化计算**：给出了参与度 E 的完整计算公式（含 E_hist、E_len、E_new 三个分量）和 PRS 综合计算公式 $PRS = min(1.0, 0.40E + 0.35V + 0.25A)$，说明了各权重参数的经验性来源；
   - **Dynamic-RAG 三级调度策略**：设计了完整的策略对照表（表 4-1），涵盖 PRS 区间、干预深度、检索偏好权重和核心调度策略；
   - **意图分类与角色调度**：列举了 11 种临床意图分类及其关联疗法维度，说明了五类 AI 治疗角色（Supportive、Empathetic、Socratic、Guided、Crisis Mode）的调度逻辑和 dynamic_switcher 边界区间策略；
   - **认知投票评分模型**：给出了每轮投票综合得分的计算公式 $CV_n = min(1.0, Σ0.1 + ΣΔ_i)$，说明了情绪-题目匹配的语义过滤规则；
   - **行为打卡与动态画像**：说明了四种打卡类型和画像修正算法（指数加权移动平均）的工程实现。

4. 针对本周计划4，完成了系统整体联调与问题修复。修复了以下测试中发现的问题：
   - SSE 连接在长消息场景下偶发的断连问题，通过增加心跳机制（每 30 秒发送一次 ping 事件）得到改善；
   - 多会话场景下，历史消息加载顺序错误的问题，通过修正 SQL 查询结果的内存反转逻辑得到修复；
   - 前端情绪图表在数据为空时显示异常的问题，通过增加空数据友好提示和默认值处理得到解决；
   - JWT Token 刷新机制缺失的问题，通过延长 Token 有效期至 7 天并增加刷新接口得到改善；
   - 管理控制台资源管理面板分页参数传递错误的问题，通过修正前端分页组件的 pageSize 传参得到修复。

### 三、存在问题

1. 问题1：论文第二章的文献综述部分尚需进一步扩充。部分引用文献的原文获取存在困难（如 medRxiv 的预印本），需要补充更多可直接引用的正式发表文献。解决尝试：增加了对 PMC 开放获取文献的引用，并补充了 Frontiers、Zenodo 等平台的公开资料。

2. 问题2：第四章核心算法部分涉及的数学公式较多，在 LaTeX 排版时出现了部分公式跨页断行不当的问题。解决尝试：使用 LaTeX 的 `\\` 命令手动调整了关键公式的断行位置，确保公式的完整性和可读性。

### 四、下周计划

1. 完善论文第五章"AI模型接入策略与选型分析"的撰写，补充多模型接入架构和配置管理；
2. 完善论文第六章"系统安全性框架设计"的撰写，补充 Guardian/Reflector 的详细实现说明；
3. 完善论文第七章（系统评估实验）的数据呈现，补充可视化图表和实验结果表格；
4. 撰写论文第八章"结论与展望"，总结研究成果并展望未来研究方向。

---

## 第11周

**工作时间段：2026.5.18 — 2026.5.23**

### 一、本周计划及执行情况

（继承第10周"下周计划"）
1. 完善论文第五章"AI模型接入策略与选型分析"：完成
2. 完善论文第六章"系统安全性框架设计"：完成
3. 完善论文第七章"系统评估实验"数据呈现：完成
4. 撰写论文第八章"结论与展望"：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了论文第五章"AI模型接入策略与选型分析"的撰写。分析了系统采用的多模型接入策略架构：
   - **统一 OpenAI 兼容接口规范**：所有接入模型（DeepSeek / OpenAI / Kimi / 本地 LoRA）均遵循 OpenAI Chat Completions API 规范，后端 AiServiceImpl 通过动态加载 ai_model_config 表中的配置参数（api_url、api_key、model_name、temperature、max_tokens）发起统一的 HTTP 调用，实现模型无关的调用封装；
   - **本地 LoRA 微调模型接入**：预留了基于 LLaMA-Factory 的本地部署接口，当本地模型部署完成后，只需在 ai_model_config 表中新增一条配置记录即可无缝接入，无需修改代码。补充了 LoRA 微调配置说明（基于 Qwen2.5-7B-Instruct，rank=16，alpha=32，dropout=0.05）和数据清洗流程（两阶段清洗：阶段一保留率 92.66%，阶段二总保留率 91.47%，最终产出 41,001 条高质量语料）；
   - **模型横向对比分析**：基于第9周实验数据，对三款模型的差异化特征进行了深入分析，揭示了"认知-情感鸿沟"现象和 Guardian 危机拦截率的模型无关性。

2. 针对本周计划2，完成了论文第六章"系统安全性框架设计"的撰写。系统描述了系统的三层纵深防御体系：
   - **Guardian 四层危机检测管道**：详细描述了四层 OR 逻辑检测的技术实现（表 6-1），包括 PHQ-9 语义层（16 项关键词组合）、BGE 向量语义层（768 维余弦相似度，阈值 0.55）、关系脉络门控层（第一人称代词 + 14 项高危情境词共现检测）、Aho-Corasick 关键词硬匹配层（高危 16 项/中危 10 项/低危 6 项分层检测）。论证了四门 OR 逻辑短路求值的确定性和与模型推理质量的解耦特性；
   - **Reflector 四维输出合规审计层**：详细描述了流式正则匹配审计的四个维度（$D_1$ 自伤建议/$D_2$ 医疗处方/$D_3$ 歧视语言/$D_4$ AI 伦理违规），说明了安全替代文本的注入机制（含全国心理危机干预热线 400-161-9995）；
   - **数据安全与隐私保护设计**：说明了 BCrypt 密码加密、JWT 无状态鉴权、匿名化设计（无需收集 PII）、敏感配置隔离（secrets.properties）、训练数据源头把控（contact/persona/authority/diagnosis 四类违禁词硬过滤）等机制；
   - **危机预警工单机制**：描述了 crisis_alert 表的完整闭环（自动生成预警记录 → 管理员人工跟进处置 → 记录处理时间和备注）。

3. 针对本周计划3，完成了论文第七章"系统评估实验"的数据呈现完善。设计并绘制了多模型综合评估结果对比表（表 7-1），涵盖 MentalAlign 双轴评估（CSS/ARS）、HEART 五维评估（Happiness/Engagement/Adoption/Retention/Task Success）、安全性能评估（危机拦截率）、综合评分与排名。补充了实验结果分析章节的撰写，深入讨论了以下核心发现：
   - 三款模型的差异化性能特征（GPT-4o Mini 认知-情感双优、DeepSeek V3 情感-留存悖论、Kimi-K2 均衡中庸定位）；
   - Guardian 危机拦截率的模型无关性验证（85.0% 为结构不变量）；
   - 认知-情感鸿沟现象的实证复现（CSS 领先 62.1% 但 Happiness 落后 0.067 分）；
   - 系统局限性的客观说明（ARS 绝对水平受限、PRS 冷启动困境、Guardian 隐喻盲区、评估样本规模限制）。

4. 针对本周计划4，完成了论文第八章"结论与展望"的撰写。总结了本研究的主要贡献：
   - 提出了基于 PRS 的情感感知动态干预机制（三段阈值 scaffold/supportive/reflective）；
   - 构建了临床意图驱动的多级治疗角色调度体系（11 种意图 × 5 种角色）；
   - 设计了 Guardian 四层 OR 确定性危机检测管道与 Reflector 四维输出合规审计层；
   - 设计了情感触发型资源库动态 RAG 调度机制；
   - 构建了 MentalAlign + HEART 双轨评测体系；
   - 基于 Spring Boot 3.3 + Vue 3 构建了高可维护工程架构。
   展望了五个未来研究方向：专业心理对话数据的 SFT 微调、多模态数字表型引入、LoRA 模型推理效率优化、Guardian 隐喻召回增强、联邦学习与本地差分隐私。

### 三、存在问题

1. 问题1：论文全文的格式统一性需要进一步检查。部分章节的图表编号、参考文献引用格式、术语使用存在不一致之处（如"心晴Pro"和"MentalAlign"的混用）。解决尝试：对全文进行了通读检查，统一了术语使用规范（正文用"MentalAlign"，系统名称用"心晴Pro"），修正了图表编号的连续性问题。

2. 问题2：第六章安全性框架设计中，部分技术细节的描述可能过于工程化，与学术论文的风格要求存在一定差距。解决尝试：与指导教师沟通后，在描述技术实现细节时增加了学术背景和理论依据的铺垫，使工程实现与理论支撑相互呼应。

### 四、下周计划

1. 完成毕业论文全文的通读与润色，统一全文格式和写作风格；
2. 完善系统使用说明书和操作演示视频脚本；
3. 整理项目源代码、文档和实验数据，确保归档完整；
4. 进行论文答辩预演，准备答辩 PPT。

---

## 第12周

**工作时间段：2026.5.25 — 2026.5.30**

### 一、本周计划及执行情况

（继承第11周"下周计划"）
1. 完成毕业论文全文通读与润色，统一格式和写作风格：完成
2. 完善系统使用说明书：完成
3. 整理项目源代码、文档和实验数据：完成
4. 进行论文答辩预演与 PPT 准备：完成

**本周计划完成度：100%**

### 二、实际工作

1. 针对本周计划1，完成了毕业论文全文的通读与润色。对全文 8 个章节（约 2.5 万字）进行了系统性的通读与修订：
   - **摘要部分**：精炼为约 300 字，涵盖研究背景、核心系统架构、主要技术方案和关键实验结论；
   - **正文各章节**：检查了逻辑连贯性，确保从研究背景→理论基础→系统设计→核心实现→安全性设计→评估实验→结论的完整叙事线；
   - **格式统一**：统一了全文术语使用（中英文混排规范）、图表编号连续性、参考文献引用格式（GB/T 7714-2015 标准）、公式编号和页眉页脚格式；
   - **语言润色**：对关键段落进行了语言优化，提升了学术表达的准确性和流畅度。补充了各章节之间的过渡段落，增强了全文的整体连贯性。

2. 针对本周计划2，完成了系统使用说明书的编写。编写了系统的完整使用文档，包括：
   - **环境准备**：JDK 21、Maven 3.8+、Node.js 18+、MySQL 8.x 的安装与配置说明；
   - **数据库初始化**：init.sql 脚本的执行步骤和注意事项（含 DROP TABLE 语句的说明）；
   - **后端启动**：Spring Boot 应用的启动方式（IDE 直接运行或 `mvn spring-boot:run`），API Key 配置说明（支持环境变量注入）；
   - **前端启动**：npm install → npm run dev 的标准流程，代理配置说明；
   - **功能操作指南**：用户注册登录、多会话对话、AI 模型切换、认知投票触发、行为打卡使用、情绪报告查看、个人中心编辑、管理后台操作等各功能模块的详细操作步骤截图和说明；
   - **故障排查**：常见问题（如数据库连接失败、API Key 无效、SSE 连接中断等）的排查思路和解决方案。

3. 针对本周计划3，完成了项目源代码、文档和实验数据的全面整理归档。整理了项目目录结构，确保三个模块（1_java_backend / 2_vue_frontend / 3_ai_engine）的代码组织清晰、README 文档完整。整理了论文相关的所有文档资料（docs/ 目录下的 9 个章节文档、实验数据、图表源文件）。检查了 .gitignore 配置，确保敏感信息（API Key、数据库密码、JWT 密钥）不会被提交到代码仓库。整理了项目依赖的第三方库和工具链清单，注明了版本号和许可协议。准备了答辩所需的演示材料（系统演示视频脚本、关键功能截图、实验数据图表）。

4. 针对本周计划4，完成了论文答辩预演与 PPT 准备。制作了 20 页的答辩 PPT，结构如下：
   - **封面页**：论文题目、答辩人信息、指导教师、答辩日期；
   - **目录页**：研究背景、系统设计、核心实现、安全性框架、评估实验、结论展望；
   - **研究背景（2页）**：高校心理健康服务供需失衡的现状、现有 AI 心理支持系统的局限性、研究目标与意义；
   - **系统总体架构（2页）**：前后端分离架构图、技术栈全景、功能模块划分；
   - **核心算法（3页）**：PRS 心理准备度得分机制、Dynamic-RAG 三级调度策略、意图分类与角色调度；
   - **安全性框架（2页）**：Guardian 四层危机检测管道、Reflector 输出合规审计层；
   - **评估实验（3页）**：实验设计与数据集、多模型横向对比结果（表格+柱状图）、关键发现与讨论；
   - **结论与展望（1页）**：主要贡献总结、未来研究方向；
   - **致谢页**。
   进行了 3 次模拟答辩预演，控制了汇报时间（15 分钟以内），预演中针对评审可能提问的问题准备了参考答案（PRS 权重设定的合理性、Guardian 隐喻盲区的解决方案、与其他心理支持系统的对比优势等）。

### 三、存在问题

1. 问题1：答辩 PPT 的动画效果过于复杂，可能影响答辩现场的演示流畅性。解决尝试：简化了 PPT 的动画效果，仅保留必要的淡入切换，确保答辩演示的稳定可靠。

2. 问题2：系统演示视频的录制时间较长（约 8 分钟），超出了答辩时间中演示环节的建议时长。解决尝试：将演示视频精简至 5 分钟左右，聚焦于最能体现系统核心创新点的功能演示（PRS 自适应干预、认知投票触发、Guardian 危机检测三个场景）。

### 四、下一步工作

1. 参加毕业论文正式答辩，根据评审意见进行论文修订；
2. 完成系统的最终部署和演示环境配置；
3. 整理答辩反馈，持续完善系统功能和论文内容；
4. 项目收尾归档，撰写项目总结报告。
